#!/bin/bash

### Build and install custom shell components
echo "Unimplemented, need to separate the components repo from the dotfiles one"
echo "TODO: clone the components repo, build and install the components"
